# Implementation Plans

Generated by the improve skill on 2026-07-27. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix orphan HTML tags in BaseLayout.astro | P1 | S | none | DONE |
| 002 | Fix empty read-more link in PostCard.astro | P1 | S | none | DONE |
| 003 | Establish verification baseline | P1 | M | none | DONE |
| 004 | Extract shared base URL utility | P2 | S | none* | DONE |
| 005 | Fix dependency vulnerabilities | P2 | S | none | DONE |
| 006 | Optimize paper screenshot images (WebP + resize) | P1 | M | none | DONE |

*Plan 004 has no hard dependency, but executing it after Plan 003 is recommended so the smoke test validates that the refactor doesn't break the build.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001, 002, 003, 004, 005, 006 are all independent and can run concurrently if needed.
- However, 004 benefits from 003's verification baseline (build smoke test) for confidence.
- 005 and 006 both install npm packages; run them sequentially to avoid lockfile conflicts.
- 006 is the highest-impact plan for perceived performance (image loading) and should be prioritized.

## Recommended execution order

1. **006** — Image optimization (your #1 complaint — 10–20× reduction in image payload)
2. **001** — Fix orphan tags (trivial, immediate improvement on every page)
3. **002** — Fix empty link (trivial, accessibility fix on every post card)
4. **003** — Verification baseline (enables confidence for future changes)
5. **004** — Base URL extraction (benefits from 003's smoke test)
6. **005** — Dependency vulnerabilities (independent, can go whenever)

## Findings considered and rejected

- **til.md frontmatter typo (`descrption`)**: Not worth a dedicated plan — it's a one-character fix that can be done opportunistically. Effort: 30 seconds.
- **Twitter widget renders on every post page**: MED confidence that the performance impact is material; the script is loaded async with `is:inline` and only runs on pages with tweets. Not worth a dedicated plan without evidence of real user impact.
- **CSS consolidation (13 files → fewer)**: MED confidence on risk/tradeoff; the current import order is load-bearing and consolidation would require visual regression checking. Worth revisiting after a verification baseline exists.
- **No linter/formatter**: MED priority for a solo blog project; the codebase is small enough that style drift is manageable. Revisit if collaborators join.
